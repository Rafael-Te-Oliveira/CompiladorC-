/* Um programa para calcular o mdc
   segundo o algoritmo de Euclides. */

int gcd (int w, int v)
{       
	if (v == 0) return w ;
	else return gcd(v,w-w/v*v);
	/* w-w/v*v == w mod v */
}

void main(void)
{	int x; int y;
	x = input(); y = input();
	output(gcd(x,y));
}
